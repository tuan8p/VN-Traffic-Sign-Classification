pipeline: svm
inherits: configs/shared.yaml

model:
  estimator: svc
  kernel: rbf
  C: 1.0
  gamma: scale
  class_weight: balanced
  probability: false

features:
  use_shared_classical: true
  pca:
    enabled: false
    n_components: 256

train:
  n_jobs: -1
  require_wandb: true
